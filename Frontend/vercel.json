{
    "version":2,
    "builds": [
        {
            "src":"Frontend/my-project/package.json",
            "use":"@vercel/static-build",
            "config": {"distDir": "dist"}
        }
    ],
    "rewrites":[
        {"source":"/(.*)", "destination":"/"}
    ]
}